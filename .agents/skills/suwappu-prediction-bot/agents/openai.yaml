interface:
  display_name: "Suwappu Prediction Bot"
  short_description: "Repo-specific patterns and workflows for suwappu-prediction-bot"
  default_prompt: "Use the suwappu-prediction-bot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true